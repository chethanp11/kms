# Placeholder secret references for local or deployed environments.
# Do not commit real secret values here.
api_keys:
  openai: ${OPENAI_API_KEY}
  github: ${GITHUB_TOKEN}
  other_services: ${OTHER_SERVICE_TOKEN}

