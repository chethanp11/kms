# Local KMS AI configuration placeholder.
# Copy this file to `.env` and replace the placeholder locally.
# Never commit a real API key.

OPEN_AI_KEY=replace-with-your-openai-api-key
KMS_AI_MODEL=gpt-4o
KMS_AI_ENABLED=true
