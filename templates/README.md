# Templates

Canonical markdown templates and structured page blueprints live here.

The wiki service reads `templates/wiki/blueprints.toml` and the page templates in `templates/wiki/*.md` to render governed drafts deterministically.
