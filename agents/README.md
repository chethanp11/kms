# Agents

Agent definitions, bounded workflow specs, and prompt assets live here.

Current structure:

- `AGENTS.md` for the global agent constitution
- `agents/*.md` for role-level agent definitions
- `skills/*/SKILL.md` for reusable procedural skills
