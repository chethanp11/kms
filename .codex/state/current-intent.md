# Current-Turn Intent

No active app-mode current-turn intent is in progress.

In app mode, this file is populated from the current user prompt by `.codex/dev_workflow/00-create-intent.md` when files will change. It must not be treated as pre-filled application intent across turns.
